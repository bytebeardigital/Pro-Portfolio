import React from 'react';
// import PropTypes from 'prop-types';

const Error = () => {
    return (
        <div>
            <h1>ERROR</h1>
        </div>
    );
};

// Error.propTypes = {};

export default Error;